# Аналіз метрик

Студент: <прізвище і GitHub-логін>. Тема: <тема>. Варіант вхідних умов: <номер>.

Заготовка з шаблону курсу. Заповніть розділи і видаліть підказки в кутових
дужках. Таблиці лежать поруч у `findings.csv`, `flow.csv` і `dashboard.csv`, а
цей файл тримає те, чого таблиці не вміщують: числа, пораховані з `flow.csv`,
пояснення складу дашборда і відповідь про накручування метрики. Умова роботи
лежить у матеріалах курсу, `lr14_task.md`, схеми трьох таблиць у
`tools/SCHEMAS.md`.

## 1. Еталонні дані курсу

| Датасет | Знахідок у `findings.csv` | Головна знахідка одним реченням |
| --- | :-: | --- |
| burndown | | |
| cfd | | |
| dora | | |

Висновок для команди delta: <два-три речення. Яке з чотирьох чисел DORA у неї
найгірше, з якою з трьох інших команд її порівнювати коректно і з якою ні, і що
ви як PM робите першим.>

## 2. Власні числа потоку

Рахуються з `flow.csv`. Метод перцентиля і формула throughput стоять у кроці 5.2
умови, рахуйте рівно за ними.

| Поле | Значення |
| --- | --- |
| Карток у файлі | <N> |
| З них із дошки ЛР4 | <N> |
| З них зі спринтів модуля M5 | <N> |
| Перша `done_date` | <дата> |
| Остання `done_date` | <дата> |
| Тижнів між ними | <N> |
| Throughput, карток на тиждень | <N,N> |
| Cycle time P50, днів | <N> |
| Cycle time P85, днів | <N> |
| Lead time P85, днів | <N> |
| Черга: lead P85 мінус cycle P85, днів | <N> |

Cycle time усіх карток за зростанням: <перелічіть числа через кому. Це той самий
ряд, з якого ви брали позиції перцентилів, і за ним ваші числа можна повторити.>

Звідки взялися дати карток зі спринтів: <одне речення. Трекер або межі спринта,
як сказано в кроці 5.1 умови.>

Що каже різниця між lead time і cycle time: <одне-два речення. Якщо черга
більша за саму роботу, скажіть, де картки чекали і що з цим робити.>

## 3. Дашборд

<Два-чотири речення. Для кого цей дашборд, чому на ньому саме ці три-п'ять
метрик і яке рішення стоїть за кожною. Аудиторія визначає зміст: спонсору
потрібні строк, гроші і ризики, вам самим потрібен потік.>

Метрика, яку я свідомо не поставив, і чому: <одне речення. Дашборд, на який усе
влізло, це не дашборд.>

## 4. Метрика, яку найлегше накрутити

| Поле | Відповідь |
| --- | --- |
| Метрика з `dashboard.csv` | <MT-0N, назва> |
| Як саме її накрутити, не зробивши роботи | <конкретна дія, а не «неправильно рахувати»> |
| Що при цьому не покращиться | <що саме лишиться незмінним у проєкті> |
| Що я роблю, щоб цього не сталося | <дія: друга метрика поруч, інший спосіб збору, відмова від метрики> |
