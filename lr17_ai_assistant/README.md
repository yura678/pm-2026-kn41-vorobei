# AI-асистент PM і межі інструмента

Студент: <прізвище і GitHub-логін>. Тема: <тема>. Варіант вхідних умов: <номер>.

Каркас роботи ЛР17. Числа і переліки лежать поруч у п'яти CSV, а тут стоїть те,
чого таблиця не вміщує: як працює ваш автоматизований крок, що показало
порівняння двох версій одного переліку, чому межі у вашому правилі саме такі, і
повний текст промптів, які ви сьогодні запускали. Заповніть розділи і видаліть
підказки в кутових дужках. Умова роботи лежить у матеріалах курсу,
`lr17_task.md`.

## 0. Інструмент і вхід

| Поле | Значення |
| --- | --- |
| Інструмент і модель | |
| Тема, чиї нотатки розбирали | |
| Скільки рядків у нотатках | <має бути 14> |
| Скільки пунктів у вашому переліку до запуску інструмента | |
| Скільки пунктів додав інструмент | |

## 1. Автоматизований крок

Опишіть крок так, щоб його міг повторити хтось інший: що подається на вхід, що
виходить, і що з виходом робить людина перед тим, як покласти його в портфель.
Три-чотири речення.

<Абзац.>

Окремо одне речення про те, коли цей крок робити не можна, тобто на яких
вхідних даних ви його не запустите.

<Речення.>

## 2. Дві версії одного переліку

Перелік лежить у `tasks.csv`, і колонка `origin` це і є дифф: `mine` там, де
пункт знайшли тільки ви, `ai` там, де тільки інструмент, `both` там, де обидва.

| Поле | Значення |
| --- | --- |
| Пунктів `mine` | |
| Пунктів `ai` | |
| Пунктів `both` | |

Що інструмент побачив, а ви пропустили, і чому ви це пропустили.

<Абзац.>

Що ви побачили, а інструмент пропустив. Назвіть тег рядка нотаток.

<Абзац.>

Один пункт, у якому ви лишили свою версію попри те, що формулювання інструмента
було гладкішим. Скажіть чому.

<Абзац.>

## 3. Помилки інструмента

Самі помилки лежать у `errors.csv`. Тут один абзац про те, яка з них коштувала б
найдорожче, якби ви її не помітили, і що саме зламалось би у вашому проєкті.

<Абзац.>

## 4. Заощаджений час

Числа лежать у `time_saved.csv`. Тут скажіть, як ви їх отримали: що заміряли
годинником, що оцінили за аналогією, і чому в кожному рядку перевірка результату
рахується окремо від генерації.

<Абзац.>

## 5. Правило роботи з AI на моєму проєкті

Саме правило лежить у `policy.csv`. Тут два абзаци.

Перший: чому рядки з режимом `forbidden` саме такі. Назвіть, що станеться, якщо
це правило порушити на вашому проєкті.

<Абзац.>

Другий: хто підписується під результатом і що це означає на практиці, коли
помилка інструмента вже пішла замовникові.

<Абзац.>

## 6. Промпти

Повний текст трьох промптів сьогоднішньої роботи, дослівно, разом із вхідними
даними, які ви в них вставляли. Скорочувати вхідні дані можна, і тоді пишіть
`<нотатки N-01 ... N-14>` замість тексту.

### Промпт запуску <P-NN>: розбір нотаток

```
<текст промпта>
```

### Промпт запуску <P-NN>: уточнення

```
<текст промпта>
```

### Промпт запуску <P-NN>: premortem

```
<текст промпта>
```
